// TODO: promote Product to a class with a getPrice method that returns a
// string of the price in the local currency.
export interface Product {
  name: string;
  price: number;
  description: string;
};
